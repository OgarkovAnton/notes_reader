from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Notes Reader reads handwritten notes and return MIDI file with recognited song',
    author='Anton',
    license='',
)
